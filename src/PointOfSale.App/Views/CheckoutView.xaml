<UserControl x:Class="PointOfSale.App.Views.CheckoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="12" Margin="0,0,8,0">
            <DockPanel>
                <TextBox DockPanel.Dock="Top"
                         Margin="0,0,0,8"
                         FontSize="16"
                         Padding="10"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Search products" />
                <ListBox ItemsSource="{Binding FilteredProducts}"
                         SelectedItem="{Binding SelectedProduct}"
                         DisplayMemberPath="Name"
                         FontSize="16" />
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding CartItems}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      FontSize="14"
                      Margin="0,0,0,8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item" Binding="{Binding Description}" Width="*" />
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80" />
                    <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat=N2}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="{Binding CartSubtotal, StringFormat='Subtotal: {0:N2}'}" FontSize="16" />
                    <TextBlock Text="{Binding CartTaxTotal, StringFormat='Tax: {0:N2}'}" FontSize="16" />
                    <TextBlock Text="{Binding CartGrandTotal, StringFormat='Total: {0:N2}'}" FontSize="20" FontWeight="Bold" Margin="0,4,0,12" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Payment:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox Width="160"
                              SelectedValue="{Binding PaymentMethod}"
                              SelectedValuePath="Content">
                        <ComboBoxItem Content="Cash" />
                        <ComboBoxItem Content="Card" />
                        <ComboBoxItem Content="MobileMoney" />
                    </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Tendered:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Width="140" FontSize="16" Text="{Binding AmountTendered, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Add Item"
                                Command="{Binding AddSelectedToCartCommand}"
                                Padding="14,10"
                                FontSize="16"
                                Margin="0,0,8,0" />
                        <Button Content="Complete Sale"
                                Command="{Binding CompleteSaleCommand}"
                                Padding="14,10"
                                FontSize="16"
                                FontWeight="Bold" />
                    </StackPanel>

                    <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,0" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
